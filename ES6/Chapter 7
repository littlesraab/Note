数组的扩展
  扩展运算符(...):实际上是rest参数的逆运算,将一个数组转为用逗号分隔的参数序列,主要运用于函数调用,即可用于代替apply方法.
  
  Array.form():把类似数组的对象(argument对象或者dom操作返回的nodelist集合)和可遍历的对象(set或map、有interator接口的数据接口)转化为真正的数组,方法可
               接受第二个参数,类似于数组的map方法,对每个元素进行处理,将处理后的值放入返回的数组,而第三个参数用于绑定第二个参数中的this.
               
  Array.of():把一组值转化为数组.
  
  copyWithin(target,start,end):在数组内部将指定位置的成员复制到其他位置并覆盖原有成员,然后返回数组,target是开始替换数据的位置,start是开始读取数据的位
                               置,默认为0,如果是负数就是倒数,end表示停止读取数据的位置.
                               
  find()/findIndex():都用于找出第一个符合条件的数组成员,它的参数是一个回调函数,所有数组成员依次执行该回调函数,直到找出第一个返回值为true的成员,然后find()
                     返回该成员,若无则返回undefined,findIndex()返回的是成员的位置,若无则返回-1,两个方法的第二个参数都用与绑定回调函数的this.
  
  fill():用给定值,填充一个数组,第一个参数为填充的数值,第二个和第三个参数用于指定填充的起始位置和结束位置.
  
  entries(),keys(),values():用于遍历数组,keys()是对键名的遍历,values()是对键值的遍历,entries()是对键值对的遍历,它们都返回一个遍历器对象,可以用for...of
                            循环进行遍历.
