数组的扩展
  扩展运算符(...):1.实际上是rest参数的逆运算,将一个数组转为用逗号分隔的参数序列.
                 2.主要运用于函数调用,即可用于代替apply方法.
  
  Array.form():1.把类似数组的对象(argument对象或者dom操作返回的nodelist集合)和可遍历的对象(set或map、有interator接口的数据接口)转化为真正的数组.
               2.方法可接受第二个参数,类似于数组的map方法,对每个元素进行处理,将处理后的值放入返回的数组,而第三个参数用于绑定第二个参数中的this.
               
  Array.of():1.把一组值转化为数组.
             2.可以用来替代Array()或new Array(),并且不存在由于参数不同而导致的重载.
             3.Array.of()总是返回参数值组成的数组,如果没有参数就返回一个空数组.
  
  copyWithin(target,start,end):1.在数组内部将指定位置的成员复制到其他位置并覆盖原有成员,然后返回数组.
                               2.target是开始替换数据的位置,start是开始读取数据的位置,默认为0,如果是负数就是倒数,end表示停止读取数据的位置.
                               
  find()/findIndex():1.都用于找出第一个符合条件的数组成员.
                     2.它的参数是一个回调函数,所有数组成员依次执行该回调函数,直到找出第一个返回值为true的成员.
                     3.find()返回该成员,若无则返回undefined,findIndex()返回的是成员的位置,若无则返回-1,两个方法的第二个参数都用与绑定回调函数的this.
  
  fill():1.用给定值,填充一个数组.
         2.第一个参数为填充的数值,第二个和第三个参数用于指定填充的起始位置和结束位置.
  
  entries(),keys(),values():1.用于遍历数组,keys()是对键名的遍历,values()是对键值的遍历,entries()是对键值对的遍历.
                            2.它们都返回一个遍历器对象,可以用for...of循环进行遍历.
  
  includes():1.返回一个布尔值表示某个数组是否包含给定的值,与字符串的includes方法类似.
             2.该方法的第二个参数表示搜索的起始位置,默认为0,如果第二个参数为负数则表示倒数的位置,如果这时它大于数组长度则会重置为从0开始.
  
  数组的空位:1.数组的空位指数组的某一个位置没有任何值,空位不是undefined,一个位置的值等于undefined是依然是有值的,空位是没有任何值.
            2.ES6是明确将空位转为undefined.
            3.由于空位的处理规则非常不统一,所以建议避免出现空位.
