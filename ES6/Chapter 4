正则表达式的扩展
  match()/replace()/search()/split():ES6全部都定义在RegExp对象上,只有RegExp实例才能用.
  u 修饰符:用来处理大于\uFFFF的 Unicode 字符,即处理四个字节的 UTF-16 编码.
  y 修饰符:也叫"粘连"修饰符,与g修饰符相似,也为全局匹配,后一次匹配都从上一次匹配成功的下一个位置开始,但要确保匹配必须从剩余的第一个位置开始就匹配.
  sticky/flag属性:sticky属性返回对象是否设置了y修饰符,flag属性返回对象的所有修饰符.
